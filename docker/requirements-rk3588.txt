# VisionCore Enterprise Edition - RK3588平台专用依赖
# ========================================================
# 针对ARM64/RK3588平台优化，排除了PyTorch相关包
# ========================================================

# 图像处理
opencv-python>=4.8.0            # OpenCV图像处理库
numpy>=1.24.0                   # 数值计算库
pillow>=10.0.0                  # Python图像处理库

# 通信协议
paho-mqtt>=2.0.0                # MQTT客户端
paramiko>=3.0.0                 # SFTP/SSH客户端

# 配置与日志
pyyaml>=6.0                     # YAML配置文件解析

# 数据处理
scipy>=1.10.0                   # 科学计算（标定算法）

# ========================================================
# 注意事项
# ========================================================
# 1. rknn-toolkit2通过wheel文件单独安装（见Dockerfile）
# 2. 不包含torch/torchvision（RK3588使用RKNN推理）
# 3. 如需ultralytics的辅助功能，需要单独处理依赖冲突
# 4. 适用于Python 3.10 (cp310)
# ========================================================

